#include<bits/stdc++.h> 

using namespace std; 

main (){ 
	int n;
	cin >> n;
	while(n--){
		int a;
		cin >> a;
		int b[a];
		int sum = 0;
		for ( int i = 0; i < a-1; i++){
			cin >> b[i];
			sum += b[i];
		}
		cout << (a+1)*a/2 - sum << endl;
		
	}
}

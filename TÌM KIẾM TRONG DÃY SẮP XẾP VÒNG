#include<bits/stdc++.h> 

using namespace std; 

main (){ 
	int n;
	cin >> n;
	while(n--){
		int a, b;
		cin >> a >> b;
		int c[a];
		for ( int i = 0; i < a; i++){
			cin >> c[i];
		}
		for ( int i = 0; i < a; i++){
			if ( c[i] == b ){
				cout << i+1 << endl;
				break;
			}
		}
		
	}
}

#include<bits/stdc++.h>

using namespace std;

int main(){
	int n;
	cin >> n;
	while ( n > 0 ){
		int a;
		cin >> a;
		int arr[a];
		int max = 0;
		for ( int i = 0; i < a; i++){
			cin >> arr[i];
			if ( max < arr[i] ){
				max = arr[i];
			}
		}
		cout << max << endl;
	n--;
	}
}


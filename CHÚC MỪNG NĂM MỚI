#include<bits/stdc++.h> 
#include<string> 

using namespace std; 
main (){ 
	int n;
	cin >> n;
	cin.ignore();
	set<string> b;
	while(n--){
		string a;
		getline(cin,a);
		b.insert(a);
	}
	cout << b.size();
}

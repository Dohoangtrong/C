#include<bits/stdc++.h> 

using namespace std; 

main (){ 
	int n;
	cin >> n;
	while(n--){
		int a;
		cin >> a;
		int b[a];
		for ( int i = 0; i < a; i++){
			cin >> b[i];
		}
		sort(b,b+a);
		if ( b[0] == b[1] ){
			cout << -1 << endl;
		}else{
			cout << b[0] << " " << b[1] << endl;
		}
		
		
	}
}

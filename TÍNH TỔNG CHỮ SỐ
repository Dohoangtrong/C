#include<bits/stdc++.h>
#include<math.h>
using namespace std;

int main(){
	int n;
	cin >> n;
	for ( int i = 0; i < n; i++){
		int m, a = 0;
		cin >> m; 
		while ( m > 0 ){
			a += m % 10;
			m /= 10; 
		}
		int b = a % 10 + a / 10;
		if ( b == 10 ){
			b = 1;
		}
		cout << b << endl;
	}
}

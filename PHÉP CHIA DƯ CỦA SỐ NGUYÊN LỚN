#include<bits/stdc++.h>

using namespace std;

int main(){ 
	int t;
	cin >> t;
	cin.ignore();
	while(t--){
		string n;
		cin >> n;
		long long m;
		cin >> m;
		long long du = 0;
		for ( int i = 0; i < n.length(); i++){
			du = ( du * 10 + ( n[i] - '0' ) ) % m;
		}
		cout << du << endl;
	}
}


#include<bits/stdc++.h> 
using namespace std; 

main(){
	int n;
	cin >> n;
	while(n--){
		int a, b;
		cin >> a >> b;
		int arr[a*a];
		for(int i = 0; i < a*a; i++){
			cin >> arr[i];
		}
		sort(arr,arr + ( a * a ) );
		
		cout << arr[b-1] << endl;
	}
}

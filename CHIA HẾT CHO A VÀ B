#include<bits/stdc++.h>
using namespace std;

main(){
	int n;
	cin >> n;
	while(n--){
		int a, b, c, d;
		cin >> a >> b >> c >> d;
		int dem = 0;
		for ( int i = a; i <= b; i++){
			if ( i % c == 0 || i % d == 0 ){
				dem++;
			}
		}
		cout << dem << endl;
	}
}


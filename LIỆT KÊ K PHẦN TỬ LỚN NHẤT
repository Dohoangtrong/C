#include<bits/stdc++.h> 

using namespace std; 

main (){ 
	int n;
	cin >> n;
	while(n--){
		int a, c;
		cin >> a >> c;
		int b[a];
		for ( int i = 0; i < a; i++){
			cin >> b[i];
		}
		sort(b,b+a);
		for ( int i = 0; i < c; i++){
			cout << b[a-1-i] <<" ";
		}
		cout << endl;
		
	}
}

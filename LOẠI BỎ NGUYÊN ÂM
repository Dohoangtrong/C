#include<bits/stdc++.h> 
#include<string>
#include<cctype>

using namespace std; 
main (){ 
	string a;
	cin >> a;
	char b;
	for (int i = 0; i < a.length(); i++){
		b = tolower(a[i]);
		if ( b != 'a' && b != 'e' && b != 'i' && b != 'o' && b != 'u' && b != 'y'){
			cout << "." << b;
		}
	}
}

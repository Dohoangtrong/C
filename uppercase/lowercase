#include<iostream>
using namespace std;

int main(){
	int n;
	cin >> n;
	for (int i = 1; i <= n; i++){
		char a;
		cin >> a;
		if ( a >= 'a' && a <= 'z'){
			a -= 32;
		} else
		if ( a >= 'A' && a <= 'Z'){
			a += 32;
		}
		cout << a << endl;
	}
}
